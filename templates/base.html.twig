<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Snowtricks{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="{{ asset('/styles2.css') }}?v={{ random() }}">
    {% block stylesheets %}

    {% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('app_home') }}">Snowtricks</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item"><div class="nav-link" href="#">Bonjour, {{ app.user.userIdentifier }}</div></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Logout</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('article_new') }}">Ajouter un Article</a></li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Se connecter</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a></li>
                {% endif %}
            </ul>
        </div>
    </nav>

    <header class="sliding-background text-center py-3">
        <div class="container">
            <h1 class="display-4"><a href="{{ path('app_home') }}">{% block header_title %}Snowtricks{% endblock %}</a></h1>
            <p>Découvrez, partagez et apprenez les figures de snowboard !</p>
        </div>
    </header>

    <main role="main" class="container mt-4">
    {# <img src="./images/snow_img.jpg"/> #}
   {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ type }} alert-dismissible fade show shadow-lg border-2 border-{{ type }} text-center" role="alert">
                <i class="bi bi-exclamation-circle-fill me-2"></i> <!-- Icône Bootstrap -->
                <strong>{{ message }}</strong>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}


        {% block body %}{% endblock %}
    </main>
    <footer class="bg-dark text-white py-4 ">
        <div class="container text-center">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5>À propos</h5>
                    <p>Snowtricks est une plateforme collaborative dédiée à la communauté passionnée de snowboard.</p>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Liens rapides</h5>
                    <ul class="list-unstyled">
                        <li><a href="" class="text-white text-decoration-none">Accueil</a></li>
                        <li><a href="" class="text-white text-decoration-none">Contact</a></li>
                        <li><a href="" class="text-white text-decoration-none">Articles</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Contact</h5>
                    <ul class="list-unstyled">
                        <li>Email : <a href="mailto:contact@snowtricks.com" class="text-white text-decoration-none">contact@snowtricks.com</a></li>
                        <li>Téléphone : <a href="tel:+33123456789" class="text-white text-decoration-none">+33 1 23 45 67 89</a></li>
                    </ul>
                </div>
            </div>
            <div class="mt-3">
                <p>&copy; {{ "now"|date("Y") }} Snowtricks. Tous droits réservés.</p>
            </div>
        </div>
    </footer>


    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
